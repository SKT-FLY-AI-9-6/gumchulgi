# APP_ENV=production 이면 JWT_SECRET 이 아래 기본값(change-me)일 때
# 서버가 부팅을 거부합니다 — 운영 배포 전 반드시 안전한 값으로 교체하세요.
JWT_SECRET=change-me
APP_ENV=dev
DATA_DIR=./data
DAILY_BUDGET_S=300
MAX_UPLOAD_MB=200
MAX_DURATION_S=180
