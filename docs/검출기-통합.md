# 검출기 통합 정리 (2026-08-14)

브랜치 6개에 흩어져 있던 검출기 사본을 **하나의 현행본**으로 통합한 기록.
결론부터: **검출기는 `psepipe_v3_seam/` 하나다.** 다른 브랜치의 검출기 파일은
전부 이것의 조상이거나 스냅샷 사본이다.

---

## 1. 브랜치 전수 조사 결과

| 브랜치 | 마지막 커밋 | 내용 | 판정 |
|---|---|---|---|
| `fix/ste-report-consistency` | **08-14** | psepipe_v3_seam (검출+보정 파이프라인 v3) + validation + IRIS 교차검증 | **검출기 최신본** → 통합 기준 |
| `jinsuk` | 08-12 | 구세대 flat 검출기 사본 + **응용층**(크롬 익스텐션, 워처, 리포트, 실시간 필터) | 검출 코어는 v3가 대체(선형광 채도비 수정 등이 v3에만 있음). 응용층은 jinsuk 고유 자산 |
| `Kim&Moon_psegpu` | 08-12 | final_detectors(08-10 스냅샷) + **psegpu_full.py**(전 GPU 파이프라인) + v3_seam.zip | GPU판은 v3 기반(`import psecore`) → 현행본 옆으로 이동. 스냅샷은 legacy로 |
| `Kim&Moon` | 08-11 | All-in-One-Deflicker 포크 + 사전학습 가중치 | **보정기** (검출기 아님) |
| `blazebvd` | 08-13 | BlazeBVD 자체 재구현 + DAVIS 학습 체크포인트(GFRM/LFRM) | **보정기** (검출기 아님) |
| `main` | — | README만 | — |

## 2. 검출기 계보 (누가 누구를 대체했나)

```
bt1702_detector.py (v0.1, 2채널, 결함 2개)
  └→ pse_analyze.py (구 팀 표준 4채널: LUM/RED/RG/BY)
       └→ pse_bt1702.py flat 시절 (jinsuk / final_detectors 사본)
            └→ psepipe_v3_seam  ← ★ 현행 (규격 특화 리팩토링: psecore/psefield/
                 │                  psepipe/tier/seam/margin + validation)
                 ├→ psegpu_full.py  (v3의 전 GPU 이식판 — 아래 3절 주의)
                 └→ pse_migraine.py (편두통 축 M1·M2, 08-14 추가, WARN 전용)
```

v3에서 **의도적으로 빠진 것**: RG/BY 색 대립축 (BT.1702 규격 외 채널이라 규격
준수 판정에서 제외 — pse_pattern.py 주석 참조). 규격 외 연구가 필요하면
`legacy_detectors/pse_analyze.py` 를 참조.

## 3. 이번 통합에서 한 일 (이 브랜치)

| 조치 | 파일 | 비고 |
|---|---|---|
| 현행본 확보 | `psepipe_v3_seam/` | fix 브랜치 병합으로 이미 반영 |
| GPU판 합류 | `psepipe_v3_seam/psegpu_full.py` | Kim&Moon_psegpu에서 이동. 의존 `pseenv.py`·`pselive3.py`·`verify_full.py`는 08-14 업로드로 확보(임포트 검증 완료). ⚠️ 남은 것: **`rawmeasure.py` 미업로드** — verify_full.py가 최상단에서 import하므로 검증 스크립트가 아직 실행 불가. 파일 헤더의 "실행 검증 안 됨" 경고는 GPU 환경에서 verify_full 통과 전까지 유효 |
| 실시간 필터 합류 | `psepipe_v3_seam/pselive3.py` | v3.1.0 — 화소별 변화율 제한(slew limit), 지연 0프레임, 자기감시 가드(악화 시 스스로 후퇴). psecore와 같은 정의 공유. **이게 게인 클램핑 계열의 현행 실시간 보정기** |
| 정직성 도구 이식 | `psepipe_v3_seam/pse_spectrum.py` | import를 pse_analyze → pse_bt1702로 교체 (상수 동일). 60fps 보간으로 판정만 통과시키는 지표 게이밍 검증용 |
| 구세대 보관 | `legacy_detectors/` | bt1702_detector(v0.1)·pse_analyze(4채널)·make_*클립 생성기. 수정 금지, 회귀 비교용 |
| 편두통 축 | `psepipe_v3_seam/pse_migraine.py` | 08-14 신규. WARN 전용 |
| **편두통 엔진 병합 (v0.2)** | `psepipe_v3_seam/pse_comfort.py` | fix의 comfort(연속 점수·4성분)를 엔진으로 확정하고 pse_migraine의 강점 3개를 이식: ① Wilkins 대역 Michelson 패턴 신호(저대비 3cpd: 2.9→12.5점), ② chroma 성분(색도분리 '교대' — 머무름과 분리), ③ t4/t5 발동점 프리셋(t4=20/t5=12, [가설]·설문 검증 대상). 합성 7클립 대조는 `validation/compare_migraine_comfort.py`. pse_migraine은 티어형 참조 구현으로 유지 |

## 4. 보정(완화) 방식 정리 — 검출기와 별개로 세 갈래

| 방식 | 위치 | 성격 | 상태 |
|---|---|---|---|
| **게인 클램핑 (규격 기반)** | `psepipe_v3_seam/psepipe.py` (+seam) | 검출된 위반 구간의 휘도 전환을 규격 안으로 눌러줌. 실시간 지향, 결정론적 | 현행. STE 리포트 일관성 수정 진행 중 (fix 브랜치명 유래) |
| **All-in-One-Deflicker** | `Kim&Moon` 브랜치 | atlas 기반 blind deflicker (CVPR'23). 영상당 학습이라 느림, 오프라인 고품질용 | 가중치 포함 동작본 |
| **BlazeBVD 자체 학습** | `blazebvd` 브랜치 | STE prior + GFRM/LFRM (ECCV'24 재구현). atlas 대비 10배 빠름 | AWS 학습 체크포인트 보유. 잔상 개선 진행 중 |

권장 역할 분담: 실시간/서비스 경로는 ① 게인 클램핑(+게인 시간축 스무딩·guided filter로
잔상 억제), 오프라인 고품질 경로는 ③ BlazeBVD, ②는 품질 비교 기준선.

## 4b. 권장 GPU 파이프라인 — A+B 하이브리드 (성능·속도 동시 확보)

핵심 원칙 세 가지:
1. **검출은 1패스, 전 축 동시** — 같은 디코드 루프에서 규격 축(psecore)과 편두통
   축(pse_migraine.Stream)을 나란히 push. 분석 폭 320px라 검출 자체는 GPU 없이도
   실시간 수십 배속. 산출물은 "구간별 위반/경고 타임라인".
2. **보정은 구간만, A 먼저 B 나중** — 계열 A(게인 제한)는 플리커의 *원인*을 규격
   임계 아래로 누르는 결정론적 처리라 빠르고 보장이 있다. 계열 B(BlazeBVD)는
   A가 남긴 잔상 구간에만 국소 적용한다. **B를 전체 영상에 돌리지 말 것** —
   비용이 수십 배이고, B 출력은 규격 통과를 보장하지 않아 결국 A/재검출이 다시
   필요하다. 순서를 뒤집으면(B 먼저) 두 배로 느려지고 보장은 그대로 없다.
3. **보정 후 반드시 재검출, 측정은 무손실로** — psepipe 사다리 구조 그대로.
   인코더 양자화가 판정을 흔들므로(rawmeasure 발견) 성능 수치는 FFV1로 잰다.

```
영상 ─→ [1패스 검출: 규격축 + 편두통축(t4/t5) 동시]
          ├─ 안전 구간 ──────────────→ 원본 그대로 (재인코딩도 생략 가능)
          ├─ PSE 위반 구간 ─→ [A: psegpu_full(GPU, verify 통과 시)
          │                     아니면 pselive3(CPU)] ─→ 재검출 ─→ 통과 → 완료
          │                                        └ 잔상 잔존 구간만
          │                                          → [B: BlazeBVD 국소] → 재검출
          └─ 편두통 WARN 구간 → 서비스 모드에 따라 경고 표시 / 완화
                                (패턴: 대비 감쇠, 색: 청색 감쇠 — 추가 개발 항목)
```

속도가 나오는 이유: 실측상 위험 구간은 영상 전체가 아니라 일부(쇼츠에서 수 초
단위)라, 가장 비싼 B가 "A 실패분 × 해당 구간"에만 걸린다. 전형 시나리오에서
전체 파이프라인 비용은 [검출 1패스 + A(위반 구간) + B(그 일부)]로, B 전체 적용
대비 한 자릿수 이상 절감.

전제조건: psegpu_full 은 GPU 장비에서 `verify_full.py` **악화 0** 통과 후에만
A 슬롯에 투입. 통과 전에는 pselive3(CPU)가 A 슬롯을 맡는다.

## 5. 앞으로의 규칙 (제안)

1. **검출 로직 수정은 `psepipe_v3_seam/` 에서만.** 다른 브랜치의 검출기 사본에
   패치를 얹지 말 것 — 이번 조사에서 세 사본이 서로 다른 수정을 들고 있었다.
2. 개인 브랜치는 "개인 검출기 사본" 용도를 끝내고, 응용층(jinsuk 익스텐션 등)·
   보정기(Kim&Moon, blazebvd)처럼 **역할 단위**로 유지.
3. GPU 경로를 살리려면 (08-14 갱신): ~~pseenv·pselive3·verify_full 푸시~~ 완료.
   남은 순서 — ① `rawmeasure.py` 푸시 ② 합성 클립 생성(`legacy_detectors/make_testclips.py`,
   synth/·genre/ 폴더) ③ GPU 장비에서 `python verify_full.py` → **악화 0이 절대 조건**
   ④ 통과하면 psegpu_full을 현행 실행판으로 승격.
4. 이 브랜치(claude/direction-feedback-review-txg5t7)를 fix 브랜치 또는 main으로
   머지하면 통합이 완성된다.
