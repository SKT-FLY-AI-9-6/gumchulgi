# seunghoon — A 필터가 움직임을 플래시로 오인하는 문제

`Kim&Moon` 의 컷 불응기가 **"필터가 안 켜지는"** 문제를 고쳤다면, 이 브랜치는
그 반대편 **"필터가 잘못 켜지는"** 문제를 고친다. 경로가 달라 서로 간섭하지 않는다.

**순 방향성 관문 하나로 헤일로가 1/4 이 되고, 악화 4편이 전부 사라진다.**

측정 2026-08-16~17 / RTX 3050 Laptop 4GB / torch 2.5.1+cu121 / 심판 `pse_bt1702`

---

## 1. 무엇이 문제였나

`psecore`(필터가 쓰는 검출기)에는 **순 방향성이 없다.** `pse_bt1702`(심판)에는 있다.

화소 단위로 보면 팬과 플래시는 구분되지 않는다. 카메라가 움직이면 어두운 벽에
있던 화소 자리에 밝은 창문이 들어오고, 그 ΔY 는 플래시의 정의를 그대로 만족한다.

심판은 `net = 밝아진면적 - 어두워진면적` 으로 막는다. 팬은 양쪽이 맞먹어
상쇄된다(실측 0.239 / 0.237 → 0.089 vs 점멸 1.000). 필터는 대신 위상상관
움직임 보상으로 막는데, `psecore` 자신이 한계를 적어 두었다:

> "움직임 보상은 전역(팬) 한정이다. 국소 물체 움직임은 보상되지 않는다."

줌·회전·**피사체 움직임**에서는 뚫린다. 숏폼은 인물이 크게 움직이고 줌이 잦아
최악의 조합이다.

## 2. 고친 것 — `filters/`

`Kim&Moon` 최신본(`cut_min_gap_s` 불응기 포함) 위에 얹었다. **전부 기본 off.**

| 플래그 | 내용 | 판정 |
|---|---|---|
| `net_directional` | `up`/`dn` 면적 차가 `arm_area` 미만이면 개입하지 않는다 | **채택 권장** |
| `net_hyst` / `net_hold` | 관문 진동 억제 (임계 / 시간) | 기각 — 아래 4절 |
| `warp_alpha` | 알파맵을 `prevV`·검출기 링과 같은 이동량으로 워프 | 기각 (−1%) |

`PeakValley.step` 이 이미 `(up, delta, down|up)` 을 반환하는데 기존 코드는
첫 번째만 쓰고 하강분을 버리고 있었다. 재료가 이미 있었다.

## 3. 측정 — 불응기 위에서 재측정한 값 (`results_rebased.csv`)

앞선 70편·10편 측정은 **불응기 이전** 코드에서 나왔으므로, 대표 8편을
`Kim&Moon` 최신본 위에서 다시 쟀다.

| 조합 | 헤일로 평균 | 악화 |
|---|---|---|
| 기준 | 35.59 | **4/8** |
| **순방향** | **9.40** | **0/8** |

**악화 4편이 전부 사라진다.** 원본이 규격 적합인데 필터가 패턴 위반을 만들던
클립들이다 — 필터가 아예 개입하지 않는 것이 정답이었다.

| 클립 | 기준 | 순방향 |
|---|---|---|
| `DbhPStxi7eE` | 패턴 / 37.14 | **적합 / 0.00** |
| `DZ3fRSEvFoi` | 패턴 / 46.04 | **적합 / 0.00** |
| `DbvE_Zohmv6` | 패턴 / 41.83 | **적합 / 4.53** |
| `Dbs05gpxSuY` | 패턴 / 18.91 | **적합 / 3.65** |
| `Db2BKAWvAXs` | 적합 / 54.45 | 적합 / **2.27** |

### 미탐 검증 (`results_diag.txt`)

진짜 플래시를 놓치지 않는다.

| 클립 | 원본 | 순방향 |
|---|---|---|
| `cera_640` | 플래시 | **적합** (마스크 0.4002 → 0.3503, 314/507 차단) |
| `Db2D03pxZjy` | 플래시 | **적합** (헤일로 9.85 → 5.93) |

움직임 클립에서는 마스크가 통째로 꺼진다(`DZ3fRSEvFoi` 0.3594 → **0.0000**).
**관문이 무차별적이지 않다.** 속도 비용 14.0 → 22.7 ms/frame.

## 4. 기각한 것 — `net_hyst` / `net_hold` (`results_hyst.csv`)

`Db2LyhvyHI5` 은 순 방향성에서 헤일로가 **역행**하는 유일한 클립이다
(22.74 → 26.93). 원인은 규명됐다 — 관문이 임계 근처에서 진동해 마스크가
깜빡이고, **헤일로는 면적이 아니라 "새로 생긴 경계"를 재기 때문**이다.

`results_diag.txt` 의 토글 측정:

| 조합 | 토글 | 헤일로 |
|---|---|---|
| 순방향 | 13회 | 26.93 |
| 기준 | 5회 | 22.74 |
| 유지 | **1회** | **17.27** |

토글과 헤일로가 예외 없이 단조다. 그런데 `net_hold` 는 **1편을 고치고 2편에서
잃는다** — `Db155zGxJRf` 27.62 → 35.58, `Db2BKAWvAXs` 1.12 → 2.91. 관문을
붙잡으면 차단이 줄고(643 → 388), 불필요한 개입이 되살아난다.

임계 히스테리시스(0.7 / 0.5)는 세 클립에서 ±1% 라 무의미했다.

**→ `net_directional` 단독이 최선. `Db2LyhvyHI5` 역행은 알려진 한계로 남긴다.**

## 5. 배경 — 릴스 70편 3파전 (`results_reels_3way.csv`, `reels_3way_report.html`)

이 조사의 출발점. **주의: 불응기 이전 코드의 측정이다.**

Explore 무작위 100편 중 70편을 원본 해상도로 `A / D_ste / A→D_ste` 에 통과시켰다.

- 악화 A 4편 / D_ste 3편 / 체인 5편 — 전부 ③패턴 축
- 헤일로 A 중앙 18.67 vs D_ste 11.12. A 가 더 나은 클립은 20/70
- 체인은 61/70 에서 두 단독보다 나쁘다

원본 위반 10편이 **전부 패턴**이고 플래시가 0편이었다.

## 6. 아직 안 된 것

- **합성 27클립 회귀 검증** — 기본값 승격 전에 필요하다
- **실사 전수 재측정** — 위 8편은 대표 표본이다. `Kim&Moon` 의 `eval_real.py`
  로 209편에 돌리면 `net_directional` 의 실제 값어치가 확정된다
- `Db2LyhvyHI5` 역행 — 클립별로 켜고 끄려면 새 판단 로직이 필요하다

## 7. 대역 에너지 검증 (`results/spectrum.txt`)

판정만 좋아지고 실제 점멸이 그대로면 지표 게이밍이다. `pse_spectrum` 으로 확인했다.

| 클립 | 3~30Hz 원본 대비 |
|---|---|
| `Db2D03pxZjy` 기준 | 17.2% |
| `Db2D03pxZjy` 순방향 | **17.8%** |
| `cera_640` 필터 | **27.3%** |

**점멸이 실제로 1/4~1/6 로 떨어진다.** 순 방향성을 켜도 17.2 → 17.8% 로
억제력을 거의 잃지 않으면서 헤일로만 줄인다.

그리고 이것이 순 방향성의 정당성을 확정한다 — 원본이 적합인 `DZ3fRSEvFoi` 에서

| | 3~30Hz 원본 대비 |
|---|---|
| 기준 | 29.0% ← 없던 위험을 없애려 에너지를 71% 깎았다 |
| 순방향 | **100.5%** ← 안전한 원본을 그대로 통과시킨다 |

## 8. 잔상 — `detail_sigma` 를 키우면 준다 (`results_detail.csv`)

실사에서 눈에 띄는 부작용은 헤일로보다 **잔상(유령 얼굴)** 이다. 기전은
`out = prev + k·d` 의 상한 S 가 프레임당 0.04(선형광)라, 마스크 안의 인물도
같이 묶여 못 따라오는 것이다.

`detail_sigma` 가 이미 그 대책이다 — **저주파(레벨)만 제한하고 질감은 현재
프레임에서** 가져온다. 문제는 기본값 2.0 이 너무 작아 수십 픽셀 규모인 얼굴이
통째로 '저주파'로 분류되는 것이다.

| sigma | `Db2D03pxZjy` 헤일로 | 점멸 | `cera_640` 헤일로 | 점멸 |
|---|---|---|---|---|
| **2** (현재) | 5.93 | 17.8% | 16.09 | 6.0% |
| 8 | 3.11 | 18.3% | 5.59 | 6.2% |
| 16 | 1.92 | 18.8% | 3.98 | 6.5% |
| **32** | **1.01** | 19.8% | **2.71** | 6.7% |

**헤일로가 1/6 이 되는데 점멸 억제는 2%p 만 손해본다.** 8/8 적합, 악화 0.
마스크 면적은 전혀 변하지 않는다(0.2652 고정) — 개입 범위는 그대로 두고 그
안에서 질감만 현재 프레임에서 가져오기 때문이다.

근거는 규격이다. BT.1702 의 플래시는 화면 1/4 이상 면적을 요구한다 — 위험
신호는 본질적으로 아주 낮은 공간주파수고, 얼굴·질감은 그보다 높은 대역이다.

**아직 기본값을 바꾸지 않았다.** `pselive3.py` 주석이 경고하듯 `tex` 를
[0.25,4] 로 자르므로 sigma 를 키우면 고대비 경계에서 항등이 깨질 수 있고
(과거 악화 2건의 원인), 이번 2편에는 줄무늬가 없었다. **패턴 클립을 포함해
더 넓게 스윕해야 승격할 수 있다.**

## 9. 실행

```bash
python check_setup.py
```

**이것부터 돌릴 것.** 없는 것과 할 일을 전부 알려준다 — 패키지, ffmpeg,
옆 브랜치 모듈, 필터에 두 수정이 다 들어 있는지, 영상 경로까지.

이 브랜치는 **단독으로 돌지 않는다.** 검출기·이질감 자가 다른 브랜치에 있다:

```bash
git clone -b fix/ste-report-consistency <repo> base
cp base/psepipe_v3_seam/*.py .
cp filters/*.py harness/*.py .        # filters 가 base 보다 최신이다
```

영상은 팀 규칙상 커밋하지 않으므로 환경변수로 알려준다:

```bash
set PSE_FLAGGED=<s1_flagged 폴더>
set PSE_EXPLORE=<explore_100 폴더>
set PSE_CERA=<cera_640.mp4>
set PYTHONIOENCODING=utf-8       # 없으면 출력 단계에서 cp949 로 죽는다
```

| 스크립트 | 내용 | 시간 |
|---|---|---|
| `run_rebased.py` | 대표 8편 — 순 방향성 효과 | 약 45분 |
| `run_detail.py` | `detail_sigma` 스윕 — 잔상 | 약 20분 |
| `diag_regen.py` | 회귀 + 마스크 토글 진단 | 약 5분 |
| `run_hyst.py` | 히스테리시스 변형 (기각됨, 재현용) | 약 15분 |

`results/logs/` 는 위 표들의 원본 출력이다.
