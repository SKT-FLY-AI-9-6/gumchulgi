# gumchulgi

광과민성 발작(PSE) 검출기 — 각자 만든 검출기를 **개인 브랜치**에 올린다.

## 브랜치 규칙

`main` 은 비워 둔다. 작업은 자기 이름 브랜치에서 한다.

| 브랜치 | 담당 |
|---|---|
| `seunghoon` | 화소별 WCAG 3조건 · BT.1702 · 모션 보정 하이브리드 |

```bash
git checkout main
git checkout -b 내이름
# 작업
git push -u origin 내이름
```
