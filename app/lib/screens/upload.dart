// app/lib/screens/upload.dart (F9 에서 교체)
import 'package:flutter/material.dart';
class UploadScreen extends StatelessWidget {
  const UploadScreen({super.key});
  @override
  Widget build(BuildContext c) => const Center(child: Text('업로드'));
}
