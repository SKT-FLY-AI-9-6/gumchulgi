// app/lib/screens/mypage.dart (F9 에서 교체)
import 'package:flutter/material.dart';
class MyPageScreen extends StatelessWidget {
  const MyPageScreen({super.key});
  @override
  Widget build(BuildContext c) => const Center(child: Text('내 페이지'));
}
