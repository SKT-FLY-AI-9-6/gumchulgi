# 전체구조 테스트 — GPU 노트북 실측 (2026-08-15)

`docs/GPU-노트북-실행-가이드.md` (브랜치 `fix/ste-report-consistency`) 의 0~3단계를
RTX 4090 Laptop 에서 전부 돌린 기록. 가이드가 "GPU 가 있어야만 되는 나머지 4개"로
남겨둔 것들이다.

환경: RTX 4090 Laptop (16GB) / torch 2.11.0+cu128 / CUDA True / ffmpeg 7.1 /
blazebvd 0.2.0 / Python 3.10.11 / Windows 11

가이드는 27클립 세트가 "리포에 없다"고 했으나 **작성자 노트북에 있었다**
(`pse_gpu/{synth,genre,run3}`). `--clip` 대체 없이 전수로 돌렸다.

---

## 1. psegpu_full 검증 — 컷 게이트 버그를 찾아 고쳤다

### 처음 결과 (수정 전)

| | |
|---|---|
| CPU 와 판정 일치 | 24 |
| 불일치 | 2 (`22_game_hud_7hz`, `seg6`) |
| **악화** | **1** (`14_stripes_drift`) |

→ `results/verify_full_27_gpu.log`

### 원인 1 — 컷 판정을 잘못된 도메인에서 쟀다 (수정함)

CPU 기준판 `pselive3._is_cut` 은 **감마 부호화된 8비트 그레이**에서 잰다
(`cv2.COLOR_BGR2GRAY`, Rec.601). GPU 판은 **선형광 Rec.709 루마**에서 쟀고
임계는 CPU 용 `cut_thresh=0.45` / `flat_sd=6.0` 을 그대로 썼다.

감마는 비선형이라 `/255` 환산으로 못 메운다 — NCC 값 자체가 달라진다.
선형광에서는 밝은 프레임의 분산이 과장돼 연속 프레임 상관이 실제보다 낮게 나오고,
그래서 컷이 과검출됐다. **22번 55회.** 컷마다 `prev` 가 리셋되니 시간축 평활이
아예 누적되지 않아 **출력이 원본과 완전히 같아졌다**(6.80s → 6.80s).

수정: 컷 판정만 CPU 와 같은 감마 도메인 Rec.601 그레이(0~255)에서 잰다.
`srgb_eotf` 가 이미 만드는 전해상도 float 중간값을 재사용해 변환을 두 번 하지 않는다.

**프레임 단위 대조** (`harness/cutdiff.py` — 숫자가 아니라 같은 프레임에서
같은 판단을 하는지 본다):

| 클립 | CPU 컷 | GPU 컷 | 일치 | GPU 과검출 | GPU 미검출 |
|---|---|---|---|---|---|
| seg6 | 26 | 27 | 26 | 1 (65번) | 0 |
| 22_game_hud | 0 | 0 | — | 0 | 0 |
| 21_anime_cuts | 0 | 0 | — | 0 | 0 |

수정 후 전수: **일치 25 / 불일치 1 / 악화 1** → `results/verify_full_27_gpu_fixed.txt`
(페더 0 까지 적용한 최종본에서는 seg6 도 0.00 PASS 가 되어 **일치 26 / 불일치 1 /
악화 1** → `results/verify_full_27_gpu_feather0.txt`)

seg6 잔여(0.58s → 0.16s)는 26개 중 한 프레임을 더 잡는 데서 온다. 축소 경로가
전해상도→64 대 전해상도→분석해상도→64 로 미세하게 다르다.

속도 비용: **+0.66 ~ +0.77 ms/frame (+4~11%)**. 헛된 컷이 있던 22번은 오히려
**37.8% 빨라졌다**.

### 원인 2 — `--cut-thresh` 플래그가 죽어 있었다 (수정함)

CLI 가 `OptF.cut_thresh` 에 쓰는데 `_cut_gate` 는 `Cfg.cut_thresh` 를 읽었다.
6개 값으로 스윕해도 `cuts` 가 55 로 고정이라 발견했다. 이제 `Cfg` 에 쓴다.

### 원인 3 — 14번 악화: 페더링. CPU 는 해결, GPU 는 잔존

`pselive3.py` 헤더 **지뢰 6** 이 이미 지목하고 있었다: `0<A<1` 인 페더 띠의
실제 변화는 `A·k + (1-A)` 라 상한을 넘고, 그 반쯤 처리된 띠가 **넓게 이어지면**
면적 규칙을 그대로 만족시켜 없던 위반이 생긴다.

27클립 전수 스윕 (악화 = 안전한 원본을 위반으로 만든 클립 수):

| 페더 | 알파보정 | 악화 | 남은 위반초 |
|---|---|---|---|
| **5.0** | off ← 기존 기본값 | **1** (14번 0.00→1.54) | 1.54 |
| 2.0 | off | 0 | 0.16 |
| 1.5 | off | 0 | 0.16 |
| **1.0** | off | **1** (0.00→0.94) | 1.06 |
| **0.5** | off | **1** (0.00→1.20) | 1.20 |
| **0.0** | off | **0** | **0.00** |
| 5.0 / 2.0 / 3.0 | on | 0 | 0.00 |

**비단조다.** 1.5·2.0 이 통과하는 건 이 코퍼스에서 우연히 면적 규칙을 안 넘은
것이고, 0 만이 페더 띠라는 기전 자체를 없앤다. (`alpha_smooth` 때문에 시간축
부분 알파는 남으므로 완전한 증명은 아니다 — 새 영상에서 재확인할 것.)

`alpha_compensate=True` 로도 막히지만 마스크 면적이 넓어져 **선명도가 80%→46%**
로 깎인다(01번). 페더 값과 무관하게(2.0·3.0·5.0 전부 46%) 알파보정 자체의 비용이다.
그래서 `feather_px = 0.0` 을 택했다. 대가는 마스크 경계가 딱딱해져 헤일로가
느는 것 (22번 13.0 → 17.5, seg6 43.2 → 47.6).

→ `results/sweep_feather.txt`, `sweep_feather_fine.txt`, `quality_feather*.txt`

**결과: CPU 기준판은 27클립 전부 악화 0** (14번 1.54 → 0.00).
**GPU 는 14번만 1.14 로 남는다** — 같은 페더 0 인데도 다르다.

GPU 잔존분에 대해 **아닌 것으로 확인된 것들** (다음 사람이 다시 파지 않도록):

| 가설 | 결과 |
|---|---|
| 움직임 보상 부재 (`warped=0`) | **아님.** 게이트를 완전히 열어 22회 워프해도 1.60 으로 더 나쁨. `motion_comp=False` 는 1.54 |
| 자기감시 가드(`gain`) 부재 | **아님.** CPU 는 가드를 꺼도 페더 0 이면 0.00 |
| 축소·선형화 순서 (감마 vs 선형) | **아님.** 감마 도메인 축소로 바꿔도 마스크 0.6464 로 동일 |
| `arm_area` 게이트 의미 (전역 스칼라 vs 화소별) | **아님.** 전역 게이트로 바꿔도 14번 변화 없음 |
| 마스크 면적 차이 | **아님.** CPU 0.610 vs GPU 0.646 로 근소 |
| "결맞은 전역 휘도 스윙" | **아님.** CPU 가 프레임간 스윙이 **더 큰데도**(0.00467 vs 0.00303) 통과한다. 심판은 전역이 아니라 국소 전이 + 창 면적으로 센다 |

**실제로 남은 단서**: GPU 가 원본을 CPU 의 2배로 바꾼다 (8비트 평균 절대차
24.21 vs 12.21). 평균 휘도도 0.395 → 0.315 (−20%) 로 떨어뜨린다 (CPU 는 −5.6%).
GPU 가 훨씬 세게 누르고 있고, 그 지속적 어두워짐이 개입 구간 경계에서 큰 전이를
만든다. 다음은 `kmax`·슬루 적용부(`out = base + (A·kmax + (1-A))·d`)와
질감 복원(`k_det`)을 CPU 와 수치 대조할 차례다. → `harness/diverge14.py`

`GPU_FULL.md` 가 "제일 의심"한 26번 위상상관 부호는 **문제 없었다** (PASS).

또 심판 둘이 다른 말을 한다는 점을 기록해 둔다. `pse_bt1702` 로 재면 14번은
**원본이 이미 패턴 위반**(③쌍수 200%, ③면적 248%)이고 A 출력도 완전히 동일하다
— A 는 패턴 축(작동기 B)을 건드리지 않는다. `psecore` 로 재면 원본 PASS,
A 출력 LUM 0.77 / RGB 0.77 FAIL. **verify_full 이 말하는 악화는 후자**,
즉 A 가 없던 휘도 플래시를 만드는 쪽이다. → `harness/diag14.py`

---

## 2. 실사 릴스 A vs D_ste (원본 해상도)

| clip | 전(위반) | A후 | A헤일로+ | D_ste후 | D헤일로+ | 승자 |
|---|---|---|---|---|---|---|
| anime | 플래시,화면전환 | 화면전환 | **0.0** | 화면전환 | 19.385 | A |
| cera_khin | 플래시 | 적합 | **1.427** | 적합 | 20.209 | A |
| travis_fein | 플래시,화면전환 | 화면전환 | **0.939** | 화면전환 | 14.601 | A |

**A 3승 0패.** 08-14 팀 실측(`validation/ad_blazebvd_full.csv`)의 패턴이 재현된다 —
판정은 비슷하게 끝나는데 D 헤일로만 한 자릿수 크다.
→ `results/ad_reels.csv`

---

## 3. D_full GPU 속도 — 실시간의 11배 느림

| clip | 해상도 | 프레임 | 영상 | GPU 실측 | ms/frame | fps |
|---|---|---|---|---|---|---|
| cera_khin | 360×640 | 507 | 16.9s | 189.1s | 373 | 2.68 |
| travis_fein | 638×360 | 931 | 38.8s | 345.9s | 372 | 2.69 |
| anime | 646×360 | 1767 | 58.9s | 601.5s | 340 | 2.94 |

CPU 기준(180p 4초 ≈ 18분 = 9000 ms/frame) 대비 화소 4배인 360p 에서 370 ms/frame
— 화소 환산 약 100배. **오프라인 국소 청소부로만 성립하고 라이브 경로는 불가능.**

원본 해상도(720×1280)는 **CUDA OOM**(11.59 GiB 요청). 팀 08-14 실측도 360 체인이었다.
→ `results/dfull_gpu_time.csv`

---

## 4. D_ste vs D_full — 신경망 단이 2/3 에서 진다

같은 360p, 같은 심판·같은 이질감 자로 다시 쟀다. (2단계 D_ste 는 원본 해상도,
3단계 D_full 은 360p 라 서로 비교할 수 없었다.)

| clip | D_ste 판정 | D_ste 헤일로 | D_ste 시간 | D_full 판정 | D_full 헤일로 | D_full 시간 |
|---|---|---|---|---|---|---|
| travis_fein | 적합 | 21.379 | 34.0s | 적합 | **18.133** | 345.9s |
| anime | 화면전환 | **24.152** | 53.5s | **플래시,화면전환** | 29.473 | 601.5s |
| cera_khin | 적합 | **26.985** | 21.7s | 적합 | 29.137 | 189.1s |

**anime 이 결정적이다 — D_ste 가 없앤 플래시를 D_full 이 되살린다.** 신경망 단을
태우니 규격도 헤일로도 둘 다 나빠졌다. `three_way_synth.csv` 의 "합성 04번에서
D 가 A 의 보정을 되돌렸다"와 같은 현상이 실사에서도 나온다.

비용까지: D_ste 는 **CPU 30~43 ms/frame**, D_full 은 **GPU 340~373 ms/frame**.
**10배 비싸고 2/3 에서 진다.**

이유는 가중치가 DAVIS 일반 플리커로 학습돼 우리 도메인(광과민성 플래시, A 가 남긴
게인 아티팩트)을 본 적이 없기 때문이다. 학습 분포 밖에서는 파라미터가 많은 쪽이
더 나쁠 수 있다.
→ `results/dste_360.csv`

---

## 5. A→D_full 체인 — 가이드가 남긴 질문의 답은 "아니오"

가이드: *"travis 에서 A→D-full 체인이 D-full 단독보다 헤일로를 줄이는가"*

| clip | A 헤일로+ | D_full 헤일로+ | A→D_full 헤일로+ |
|---|---|---|---|
| travis_fein | 2.131 | 18.133 | **20.442** |
| anime | 0.028 | 29.473 | **30.321** |
| cera_khin | 2.278 | 29.137 | **29.290** |

**3/3 전부 체인이 D_full 단독보다 나쁘다.** A 를 앞에 붙여도 D_full 의 헤일로가
그대로 남고 A 의 몫이 더해질 뿐이다. anime 은 체인이 A 단독보다 규칙을 더 없애지도
못하면서 헤일로만 0.028 → 30.3.
→ `results/three_way_reels_dfull.csv`

`validation/run_3way.py` 는 CPU 컨테이너 경로(`/home/user/...`)와 D_ste 에
하드코딩돼 있어 못 썼다. 심판·이질감 자만 재사용해 `harness/run_3way_dfull.py` 로
옮겼다.

---

## CPU/GPU 분업은 되돌아갈 길이 아니다

"각자 잘하는 부분에서 돌리자"는 이미 `psegpu.py` 가 한 것이다 — 검출 CPU + 적용 GPU,
1080p 15.47 ms (축소 1.65 + 검출CPU 4.07 + 적용GPU 9.75). 전 GPU 판은 8.4~11.9 ms.

병목은 각 연산의 속도가 아니라 **PCIe 왕복과 프레임당 동기화**다. 검출을 CPU 에서
하려면 프레임마다 GPU 결과를 기다려 내려받고 다시 올려야 하고, 그 비용이 "CPU 가
그 연산을 더 잘한다"는 이득보다 크다. 전 GPU 판이 빠른 이유가 동기화를 1회로 줄인
것이라 분업하면 설계를 스스로 무너뜨린다.

의미 있는 분업은 층이 다르다: **실시간 A 는 GPU, 오프라인 D 는 필요할 때만.**
CPU 판은 기준자로 남긴다 — 이번 컷 버그도 CPU 대조로 잡았다.

---

## 지금 상태 요약

| | 컷 수정 전 | 컷 수정 후 | + 페더 0 (현재) |
|---|---|---|---|
| CPU 와 판정 일치 | 24 | 25 | **26** |
| 불일치 | 2 | 1 | 1 (14번) |
| 악화 (GPU) | 1 | 1 | 1 (14번) |
| **악화 (CPU 기준판)** | 1 | 1 | **0** |
| GPU ms/frame | 8.4~11.9 | — | **6.3~8.0** |

## 다음에 할 일 (우선순위)

1. **GPU 14번 잔존 악화.** CPU 는 해결됐고 GPU 만 1.14 로 남는다. 위의
   "아닌 것으로 확인된 것들" 표를 먼저 읽고, `kmax`·슬루 적용부와 질감 복원을
   CPU 와 수치 대조하는 데서 시작할 것. 이게 psegpu_full 실행판 승격의 마지막 관문.
2. **페더 0 을 새 영상에서 재확인.** 스윕이 비단조라 27클립 최적이 일반화한다는
   보장이 없다. 헤일로 증가(22번 13.0→17.5)가 눈에 띄는지 사람 눈 확인도 필요.
3. **A 의 화면전환 축.** 실사 3편 중 2편(anime·travis)에서 A 가 이 축을 못 넘긴다.
   D 를 붙일지 A 를 고칠지가 여기서 갈린다. **여기서부터 실사 수집이 필요하다** —
   1·2 번은 기존 클립으로 된다.
4. **D 를 쓴다면 D_ste.** D_full 은 10배 비싸고 2/3 에서 진다. 파인튜닝 전까지는
   신경망 단을 켤 근거가 없다.
5. **파인튜닝은 데이터가 먼저.** 검출기 PASS 인 깨끗한 실사 수백 편이 필요한데
   현재 노트북에 실사는 6편뿐이라 착수 조건이 안 된다.

---

## 파일

| 경로 | 내용 |
|---|---|
| `psegpu_full.py` | 컷 게이트 수정본 |
| `pselive3.py` | 페더 0 수정본 (CPU 기준판 — 악화 0 달성) |
| `harness/cutdiff.py` | CPU↔GPU 컷 결정 프레임 단위 대조 |
| `harness/verify_ct.py` | `cut_thresh` 를 바꿔가며 전수 검증 |
| `harness/sweep_cut.py` | 실패 클립 `cut_thresh` 스윕 |
| `harness/sweep_feather.py` | 페더·알파보정 전수 스윕 (판정) |
| `harness/quality_feather.py` | 같은 조합들을 선명도·이질감으로 |
| `harness/diag14.py` | 14번이 어느 심판·어느 축에서 떨어지는지 |
| `harness/lumswing.py` | 전역 휘도 스윙 측정 |
| `harness/warp14.py` | 워프 게이트를 열어 14번 반응 보기 |
| `harness/diverge14.py` | CPU↔GPU 출력 발산 지점 국소화 |
| `harness/run_3way_dfull.py` | A / D_full / A→D_full 3파전 |
| `harness/dste_360.py` | D_ste 를 D_full 과 같은 360p 로 |
| `results/*.txt` `results/*.csv` | 위 표들의 원본 출력 (`.log` 는 저장소에서 무시돼 `.txt`) |

두 수정 파일은 `fix/ste-report-consistency` 의 `psepipe_v3_seam/` 에서 가져왔다.
그 브랜치에는 반영하지 않았다.

harness 는 전부 `psepipe_v3_seam/` 안에서 실행한다 (같은 폴더의 `psecore` 등을 쓴다).

## 재현 안 되는 것

27클립 세트와 실사 릴스는 영상이라 커밋하지 않았다 (리포 규칙). 세트는
`pse_gpu/{synth,genre,run3}`, 릴스는 `pse_gpu/최종!/*.mp4` 에 있다.
